export class Interest {
    constructor(
        private firstName: string,
        private lastName: string,
        private address: string,
        private cityName: string,
        private stateAbrv: string,
        private zipcode: number,
        private productName: string
    ) {

    }
}